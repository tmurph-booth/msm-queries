<h1>Eldest director</h1>

<p>The eldest director in our dataset right now is <a href="/directors/<%= @OldestDirector.id%>"><%= @OldestDirector.name%></a>, born on <%= @OldestDirector.dob.strftime("%B %e, %Y")%>.</p>

<p>(<a href="http://strftime.net/">http://strftime.net/</a> is handy for putting together the formatting strings for the .strftime method.)</p>
