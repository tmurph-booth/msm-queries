<h1>Youngest director</h1>

<p>The eldest director in our dataset right now is <a href="/directors/<%= @YoungestDirector.id%>"><%= @YoungestDirector.name%></a>, born on <%= @YoungestDirector.dob.strftime("%B %e, %Y")%>.</p>

<p>(<a href="http://strftime.net/">http://strftime.net/</a> is handy for putting together the formatting strings for the .strftime method.)</p>
